on:
    workflow_dispatch:

jobs:
    check_users:
        runs-on: ubuntu-latest
        steps:
            - name: Check users
              run: |
                echo ${{ toJson(github.event.sender) }}
