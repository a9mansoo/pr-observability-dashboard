on:
  issue_comment: 
    types: ["created"]


jobs:
  run_stuff:
    runs-on: "ubuntu-latest"
    steps:
      - name: "Echo world"
        run: "echo 'hello world'"
      
      - name: Dumb Step
        run: |
          echo "Hello ${{ toJSON(github.event.issue.pull_request) }}" > file.json
          cat file.json



  